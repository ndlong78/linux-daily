[Tweet 1] — đính kèm post-014-code.png
🔁 “Backup succeeded” chưa chứng minh bạn restore được. Lab #014 giờ tách rõ: backup → check metadata → đọc data → restore canary → verify checksum. Thread 🧵

[Tweet 2]
restic check mặc định kiểm repository structure/metadata/index. Nó KHÔNG đọc toàn bộ pack data. Full data verification: restic check --read-data.

[Tweet 3]
Không cần full read mỗi đêm. Có thể chạy --read-data-subset=5% theo lịch và full --read-data ở kỳ audit phù hợp với I/O/window.

[Tweet 4]
Repo nhiều host/path? Đừng dùng restore latest mơ hồ. Chọn snapshot ID hoặc latest --host ... --path ... để restore đúng nguồn.

[Tweet 5]
Restore drill nên dùng canary + checksum đã lưu TỪ TRƯỚC backup. Đừng diff với file live hiện tại vì live file có thể đã thay đổi hợp lệ.

[Tweet 6]
Linux schedule bằng systemd timer; FreeBSD dùng cron/periodic. Nhưng scheduler tồn tại không có nghĩa backup tốt: monitor tuổi snapshot + exit/log.

[Tweet 7]
Full lab + nguồn restic upstream: {{LINK}}
#LinuxDaily #Backup #restic #FreeBSD
