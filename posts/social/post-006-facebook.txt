⚙️ “Ansible không cần agent” không đồng nghĩa “managed node không cần runtime”.

Bài #006 được rà lại theo Ansible hiện hành:
• Hầu hết module POSIX cần Python trên managed node.
• ansible.builtin.raw không cần Python và dùng để bootstrap host tối thiểu.
• Ưu tiên ansible.builtin.package + service cho phần giao nhau giữa Ubuntu/Debian/Fedora/FreeBSD.
• community.general.pkgng và community.general.doas KHÔNG nằm trong ansible-core — cần cài/pin collection.
• --check --diff là preview có điều kiện; support phụ thuộc module/backend.

FreeBSD vẫn tách riêng ở backend: pkg/rc.d và doas nếu dùng privilege escalation.

👉 Đọc đầy đủ: {{LINK}}

#LinuxDaily #SysAdmin #Ansible #FreeBSD #Automation

[Đính kèm ảnh: post-006-code.png]
