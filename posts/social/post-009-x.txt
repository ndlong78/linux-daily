[Tweet 1] — đính kèm post-009-code.png
🔑 PR #28 sửa một lỗi logic rất hay gặp: nếu đã cho user vào nhóm admin toàn quyền rồi, rule “chỉ được chạy 1 lệnh” không còn là least privilege nữa. Thread 🧵

[Tweet 2]
Phải tách 2 role: Administrator → sudo/wheel, quyền rộng. Operator → KHÔNG vào admin group, chỉ có sudoers/doas rule command-specific.

[Tweet 3]
Ubuntu thường dùng nhóm sudo. Debian thì không nên giả định sudo luôn có: nếu installer đặt root password, sudo có thể chưa cài và user không tự vào nhóm sudo.

[Tweet 4]
Fedora dùng wheel cho quyền admin rộng qua sudo. Vì vậy operator chỉ restart nginx không nên thuộc wheel; dùng /etc/sudoers.d/ với command + arguments chính xác.

[Tweet 5]
FreeBSD: wheel liên quan su lên root. doas là package riêng và có thể cấp trực tiếp theo user + cmd + args; operator doas không bắt buộc phải vào wheel.

[Tweet 6]
Kiểm policy trước khi dùng thật: Linux dùng visudo -c và sudo -l -U alice. FreeBSD dùng doas -C để parse + command-match mà không thực thi lệnh.

[Tweet 7]
Full bài source-backed: {{LINK}}
#LinuxDaily #sudo #doas #FreeBSD
